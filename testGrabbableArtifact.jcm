mas testGrabbableArtifact {

    agent npc : grabber.asl {
        ag-class: vesna.VesnaAgent
        beliefs: address(localhost)
            port(3450)
        goals: grab_artifact
        join: w1
    }

    workspace w1 {
        artifact cylinder: artifact.GrabbableArtifact()
        artifact envManager: artifact.EnvManagerArtifact("envManager", 34)
    }
}