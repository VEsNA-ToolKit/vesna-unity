package artifact;

import artifact.GrabbableArtifact;

public class CylinderArtifact extends GrabbableArtifact {
    @Override
    public void init(String artifactName, int webSocketPort) {
        super.init(artifactName, webSocketPort);
    }
}
